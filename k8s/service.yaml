apiVersion: v1
kind: Service
metadata:
  name: crud-api-service
  labels:
    app: crud-api
spec:
  type: ClusterIP
  selector:
    app: crud-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    name: http
